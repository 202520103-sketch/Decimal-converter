# streamlit_10to2_with_info.py
import streamlit as st

st.set_page_config(page_title="10진수 → 2진수 변환기", layout="centered")

# 제목
st.title("🔢 10진수를 2진수로 변환기")

# 설명
st.markdown("""
이 웹사이트는 10진수를 2진수로 변환해주는 간단한 도구입니다.  
사용 방법은 다음과 같습니다:

1. 아래 입력창에 10진수를 입력합니다.
2. '변환' 버튼을 클릭하면 2진수 결과가 표시됩니다.

💡 예시: 10 → 1010
""")

# 입력
decimal_input = st.number_input("10진수를 입력하세요", min_value=0, step=1)

# 변환 버튼
if st.button("변환"):
    binary = bin(decimal_input)[2:]  # '0b' 제거
    st.success(f"2진수: {binary}")
